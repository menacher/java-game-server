<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">
	
	<import resource="server-protocols.xml" />
	
	<context:annotation-config />
	
	<!-- Configure all properties files here, wildcards like *.properties are 
		also allowed -->
	<context:property-placeholder
		location="jetserver/props/jetserver.properties" />
		
	<!-- The Spring application context -->
	<bean id="appContext" class="org.menacheri.jetserver.context.AppContext" />
	
	<!-- Configure the three servers. By default tcp and udp runs at 18090 and flash at 843 -->
	<bean id="serverManager" class="org.menacheri.jetserver.server.netty.ServerManagerImpl" />
	
	<bean id="tcpServer" class= "org.menacheri.jetserver.server.netty.NettyTCPServer"
		destroy-method="stopServer">
		<constructor-arg ref="tcpConfig" />
		<constructor-arg ref="protocolMultiplexerPipelineFactory" />
		<property name="gameAdminService" ref="gameAdminService" />
	</bean>

	<bean id="udpServer" class="org.menacheri.jetserver.server.netty.NettyUDPServer"
		destroy-method="stopServer">
		<constructor-arg ref="udpConfig" />
		<constructor-arg ref="updChannelPipelineFactory" />
		<property name="gameAdminService" ref="gameAdminService" />
	</bean>

	<!-- Configure the Flash policy server. By default it runs at 843 -->
	<bean id="flashPolicyServer" class="org.menacheri.jetserver.server.netty.FlashPolicyServer"
		destroy-method="stopServer">
		<constructor-arg ref="flashConfig" />
		<constructor-arg ref="flashPolicyServerPipelineFactory" />
		<property name="gameAdminService" ref="gameAdminService" />
	</bean>

	<!-- All the pipeline factory beans are now defined -->
	<bean id="flashPolicyServerPipelineFactory" class="org.menacheri.jetserver.server.netty.FlashPolicyServerPipelineFactory">
		<lookup-method name="getFlashPolicyServerHandler" bean="flashPolicyServerHandler" />
	</bean>
		
	<bean id="loginPipelineFactory" class="org.menacheri.jetserver.server.netty.LoginPipelineFactory">
		<property name="frameSize" value="1024" />
		<property name="eventDecoder" ref="eventDecoder" />
		<property name="loginHandler" ref="loginHandler" />
		<property name="lengthFieldPrepender" ref="lengthFieldPrepender" />
	</bean>
	
	<bean id="updChannelPipelineFactory" class="org.menacheri.jetserver.server.netty.UDPChannelPipelineFactory">
		<property name="udpEventEncoder" ref="udpEventEncoder" />
		<property name="upstream" ref="udpUpstreamHandler" />
	</bean>
	
	<bean id="protocolMultiplexerPipelineFactory" class="org.menacheri.jetserver.server.netty.ProtocolMultiplexerPipelineFactory">
		<property name="bytesForProtocolCheck" value="5" />
		<property name="loginProtocol" ref="compositeLoginProtocol" />
	</bean>
	
</beans>
